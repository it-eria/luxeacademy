{{#org-blocks-under-slider}}
    <section class="blocks-under-slider">
        <div class="container">
            <div class="wrapp-items-block">
                {{#col-item-wrapp}}
                    <div class="col-lg-3 col-md-6 col-6 item-wrapp">
                        <img class="m-auto d-block" src="{{{logotype}}}" alt="img"/>
                        <h3>{{{title}}}</h3>
                        <p>{{{content}}}</p>
                    </div>
                {{/col-item-wrapp}}
            </div>        
        </div>
    </section>
{{/org-blocks-under-slider}}